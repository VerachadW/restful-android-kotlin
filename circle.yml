machine:
    environment:
        ANDROID_HOME: /usr/local/android-sdk-linux
dependencies:
    cache_directories:
        - ~/.android
        - /usr/local/android-sdk-linux
    override:
        - bash install-sdk.sh
test:
    override:
        - chmod +x gradlew
        - ./gradlew clean assemble